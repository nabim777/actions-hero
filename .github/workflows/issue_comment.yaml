name: Create a comment on new issues

on:
  issues:
    types: [open]
  workflow_dispatch:

jobs:
  comment-with-action:
    runs-on: ubuntu-latest
    steps:
      - name: "dumop github context"
        run: echo '${{ toJSON(github.event)}}' | jq
        shell: bash
